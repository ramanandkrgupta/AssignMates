shorebird release android --flutter-version=3.35.6

for release for play store . must must versiion in pubspec.yaml. 

shorebird patch --platforms=android --release-version=1.0.0+4

for small patch update like text , edit ui of existing screen .

